# Navigate to where you want to clone the repo (or if you already have it locally, skip to step 2)
cd ~/Documents  # or wherever you keep your projects

# Clone your repo (if you haven't already)
git clone https://github.com/warmac57/BigClock.git

# Navigate into the repo
cd BigClock

# Copy the alarm-clock.html file you downloaded into this folder
# (Just drag and drop it into the BigClock folder)

# Add the file to git
git add alarm-clock.html

# Commit the changes
git commit -m "Add full-featured alarm clock with YouTube music and snooze"

# Push to GitHub
git push origin main